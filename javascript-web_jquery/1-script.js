$(document).ready(function() {
    // Selecciona el elemento <header> usando jQuery
    var $headerElement = $('header');

    // Cambia el color del texto a rojo (#FF0000)
    $headerElement.css('color', '#FF0000');
});
